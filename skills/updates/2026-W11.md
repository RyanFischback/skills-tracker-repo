# Weekly Skills Update

Week of: `2026-03-09`  
Owner: `Ryan Fischback`

## What This Tracker Is

This tracker shows the top skills used in production workflow and summarizes what each one contributes so execution gets faster and more reliable over time.

## Skills This Week

1. `linear`: Uses the linear skill with Linear MCP to create, update, and track tickets without context switching.
2. `figma`: Uses the figma skill with Figma MCP to pull design context and assets quickly for faster implementation.
3. `test-driven-development`: Uses failing tests first, then minimal implementation, then refactor to reduce regressions.
4. `systematic-debugging`: Uses hypothesis-driven checks to isolate root cause instead of random trial-and-error.
5. `verification-before-completion`: Requires fresh verification before saying anything is done, which improves reliability.

## Weekly Note

- First baseline week for `rfischbackdev` skills tracking. Next updates will add concrete output metrics per skill.
